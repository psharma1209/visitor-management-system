import { VisitorSearchPipe } from './visitor-search.pipe';

describe('VisitorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VisitorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
